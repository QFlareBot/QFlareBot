{
  "name": "qqbot-plugin-example",
  "version": "0.1.0",
  "description": "QQ 机器人插件示例：命令、正则与事件",
  "license": "MIT",
  "type": "module",
  "files": [
    "dist"
  ],
  "keywords": [
    "qqbot-plugin"
  ],
  "scripts": {
    "build": "qqbot-plugin build",
    "sync": "qqbot-plugin build && node -e \"require('node:fs').copyFileSync('dist/manifest.json','manifest.json')\"",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250917.0",
    "@qqbot/plugin-cli": "^0.1.0",
    "@qqbot/sdk": "^0.1.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
